--extra-index-url https://download.pytorch.org/whl/cu118

# Core libraries
streamlit==1.40.2
opencv-python==4.10.0.84
pillow==10.2.0
numpy==1.26.3
onnx==1.17.0
onnxruntime==1.20.1  # CPU version for compatibility
insightface==0.7.3
psutil==6.1.0
tk==0.1.0  # Ensure tkinter support
tensorflow==2.18.0
opennsfw2==0.14.0
protobuf==5.29.1
tqdm==4.66.5
gfpgan==1.3.8

# Optional: For tkinter-related GUI support (handle platform dependencies)
# Install the platform-specific tkinterdnd2 version:
tkinterdnd2; sys_platform != 'darwin' and platform_machine != 'arm64'
tkinterdnd2-universal; sys_platform == 'darwin' and platform_machine == 'arm64'

# Ensure that required tkinter libraries are installed:
# (For systems where tkinter might not be included)
libtk8.6==0.1.0  # This ensures tkinter dependencies are available, if needed
